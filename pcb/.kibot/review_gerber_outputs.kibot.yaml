# This script is called by the .github/workflows/gen_review_gerbers.yml workflow.

kibot:
  version: 1

preflight:
  run_erc: true

global:
  output: '%f_rev_%r-%i.%x'

# This imports the KiBot scripts that actuall do all the generation. If you want to
# generate different things/more things then change these to point to other .kibot.yaml files.
import:
  - includes/pcb/generic_gerbers.kibot.yaml